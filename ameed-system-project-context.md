# ملف تعريف المشروع — نظام العميد الإلكتروني للقاعات الدراسية

> **الغرض من هذا الملف:** تزويد محادثة Claude جديدة بكل ما تحتاج معرفته عن نظام "العميد الإلكتروني للقاعات الدراسية" القائم حالياً، لأن مشروعاً جديداً منفصلاً سيُبنى بالارتباط معه. هذا الملف **لا يشرح متطلبات المشروع الجديد** — تلك ستُشرح في المحادثة الجديدة نفسها. الهدف هنا هو نقل السياق التقني والتاريخي فقط، بما في ذلك الأخطاء التي حدثت والدروس المستفادة، لتفاديها.

---

## 1. نظرة عامة

نظام ويب بسيط (صفحة واحدة) لإدارة حجوزات وجداول القاعات الدراسية في جامعة العميد، يخدم عدة كليات بصلاحيات منفصلة لكل كلية، ويشمل: جدول أسبوعي متكرر للمحاضرات، طلبات حجز القاعات لأنشطة إضافية، إدارة بيانات التدريسيين، تقارير قابلة للطباعة (تدريسي/قاعة)، ولوحة إدارة عامة.

- **الجهة المالكة:** مشتاق كريم — مساعد رئيس الجامعة للشؤون الإدارية والقانونية، جامعة العميد، كربلاء، العراق.
- **الرابط المباشر (Live):** `https://mushtaqaelk.github.io/ameed-room-booking/`
- **المستودع (GitHub):** `mushtaqaelk/ameed-room-booking` — الفرع الرئيسي `main`، اسم الملف في المستودع هو **`index.html`** (وليس اسمه المحلي المستخدم في جلسات العمل).
- **قاعدة البيانات:** Firebase Firestore، مشروع باسم `ameed-room-booking`.
  ```js
  firebaseConfig = {
    apiKey: "AIzaSyA2QU9QroY320QrIB-Q0ns2Hjs8h63WMYE",
    authDomain: "ameed-room-booking.firebaseapp.com",
    projectId: "ameed-room-booking"
    // + storageBucket/messagingSenderId/appId داخل ملف الصفحة نفسه
  };
  ```
  (هذا مفتاح عميل Firebase عام وليس سرّاً حساساً — الحماية الفعلية تتم عبر قواعد أمان Firestore، وهو مرئي أصلاً لأي شخص يفتح مصدر الصفحة المنشورة.)

---

## 2. البنية التقنية

- **ملف واحد فقط:** `index.html` يحتوي كل شيء — HTML + CSS (داخل `<style>`) + JavaScript (داخل `<script>` واحد، JS عادي ES5-ish بدون أي بناء/bundler/framework).
- **لا يوجد build step.** أي تعديل هو تعديل مباشر على نص الملف، ثم رفعه كما هو.
- **مكتبة Firebase:** تُستخدم نسخة الـ "compat SDK" (`firebase.initializeApp`, `db.collection(...).onSnapshot(...)`, إلخ) — أسلوب قديم لكنه مبسّط ومناسب لملف واحد بدون بناء.
- **مساعد DOM بسيط:** دالة `el(tag, attrs, children)` (تُنشئ عنصر HTML وتُطبّق الخصائص/المستمعين وتُلحق الأبناء) هي الأداة شبه الوحيدة لبناء الواجهة برمجياً؛ لا يوجد أي إطار عمل (لا React ولا Vue).
- **التصميم:** RTL بالكامل، عربي، بخط Amiri/Noto Naskh Arabic للعناوين وTajawal للنص، ويدعم وضعين فاتح/داكن عبر `prefers-color-scheme` و `data-theme` على `<html>`.
- **التبويبات (tabs):** الرئيسية، الجدول الأسبوعي، طلبات الحجز، التقارير، الإدارة (تظهر فقط لمن لديه صلاحية admin).

---

## 3. نموذج البيانات (Firestore)

### `config/general` (مستند واحد يحوي كل الإعدادات المشتركة)
```
{
  colleges: [{id, name}, ...],       // med, nur, den, phar, hmt, edu
  rooms: [{id, name}, ...],          // القاعات الرسمية (53 قاعة حالياً بعد آخر تحديث إضافي)
  instructors: [{id, name, college, title, email}, ...],  // 177 تدريسي حالياً
  people: { admin, ceManager, college: {med, nur, den, phar, hmt, edu} },
  codes:  { admin, ceManager, college: {med, nur, den, phar, hmt, edu} }
}
```
- `title` أحد القيم: `''|'أستاذ'|'استاذ مساعد'|'مدرس'|'مدرس مساعد'`، ويُحوَّل لعرض مثل "أ.د." عبر `INSTRUCTOR_TITLE_PREFIX`.
- **ملاحظة:** `config.people.college` لا يزال ناقص الأسماء لكليات: `den, phar, hmt, edu` (لم تُستكمل بعد).

### `schedule` (collection) — الجدول الأسبوعي المتكرر
كل مستند = محاضرة متكررة أسبوعياً، **وليس حدثاً بتاريخ محدد**:
```
{ day, start, end, course, instructor, stage, room, college, sessionType }
```
- `day`: أحد رموز `sat|sun|mon|tue|wed|thu` (الأسبوع الدراسي سبت-خميس، الجمعة عطلة). **لا يوجد يوم "fri" في WEEK_DAYS.**
- `start`/`end`: نص "HH:MM".
- `instructor`: **نص حر** (وليس مرجع ID) — قد يحوي أكثر من اسم مفصول بفواصل/"و". هذا يعني أن تعديل قائمة التدريسيين الرئيسية لا يكسر الجدول القديم، لكن لا يوجد رابط بنيوي حقيقي بين الاثنين.
- `room`: **مرجع ID** إلى `config.rooms`، يُحل عبر `roomName(id)`.
- `sessionType`: `theory|practical|''` (يُعرض عبر `sessionTypeLabel`).
- **⚠️ نقطة مهمة جداً لأي ميزة جديدة:** هذا الجدول **نمط متكرر فقط بدون تواريخ فعلية**. لا يوجد فرق مخزّن بين "محاضرة السبت هذا الأسبوع" و"محاضرة السبت الأسبوع القادم" — كلاهما نفس السطر. أي طلب يتعلق بـ"فترة زمنية" أو "تاريخ فعلي" يتطلب منطق إسقاط (projection) يدوياً فوق هذا النمط المتكرر (مثال: انظر قسم 7 أدناه، دالة `expandRoomOccurrences`).

### `bookings` (collection) — طلبات حجز لأنشطة إضافية (مختلف عن الجدول الدراسي)
حجوزات **بتاريخ فعلي محدد** (`date` نص ISO)، بحالة `pending|approved|rejected`، لأنشطة مثل محاضرة إضافية/ورشة/دورة/اجتماع. هذا الكولكشن **هو الوحيد** الذي يحمل تواريخ حقيقية في النظام الحالي.

### الأدوار (roles)
- `admin` (الإدارة العامة) — صلاحية كاملة على كل الكليات.
- `college:<id>` — صلاحية على كلية واحدة فقط (تعديل جدولها وتدريسييها).
- `ce` (مسؤول التواصل المؤسسي؟) — صلاحية موافقة على الحجوزات (`canApprove`).
- بوابة الصلاحية الأساسية المُعاد استخدامها في كل مكان: `canEditCollege(collegeId) = session.role==='admin' || (session.role==='college' && session.collegeId===collegeId)`.

---

## 4. خارطة الكود (أهم الدوال حسب القسم، لتسريع التوجّه داخل الملف)

```
/* top bar / buttons / tabs / clock / room grid / chips / forms / modal-toast / excel import / reports (CSS) */
$(id), el(tag, attrs, children)              — أدوات DOM أساسية
fmtISODate, jsDayToCode, dayLabel, overlap    — أدوات تاريخ/وقت
collegeName(id), roomName(id)                 — عرض الأسماء من config
instructorDisplayName(inst)                   — يضيف بادئة اللقب العلمي
canEditCollege / canApprove / isAdmin         — الصلاحيات
init() → bindShell() → renderAll()            — تسلسل الإقلاع
renderHome()                                  — حالة القاعات الآن + القادم
renderScheduleTab()                           — قسم الجدول الأسبوعي (مرتّب: شرائح الكليات → جدول الأسبوع → إضافة محاضرة يدوياً → استيراد Excel → إدارة التدريسيين)
onScheduleSubmit / commitScheduleAdd / deleteScheduleEntry
extractDataRows / buildImportPreview / commitImport  — استيراد جدول من Excel
buildReportHead(titleText)                    — رأس مشترك للتقارير (شعار + عناوين) — استخدمه لأي تقرير جديد
renderInstructorReport()                      — تقرير تدريسي (بحث بالاسم)
populateRoomReportSelect / startOfCurrentWeek / initReportRoomDateRange / expandRoomOccurrences / renderRoomReport
                                               — تقرير القاعة (فترة زمنية + إسقاط تواريخ فعلية على النمط المتكرر)
renderBookingsTab / onBookingSubmit / approveBooking / rejectBooking
renderAdminTab / onAddCollege / onAddRoom / renderInstructorsManageCard / onAddInstructor / updateInstructor
configDoc() → db.doc('config/general')
```

---

## 5. اصطلاحات عمل مهمة (اتُّبعت طوال المشروع ويجب الاستمرار عليها)

1. **كل تعديل على البيانات إضافي (additive) ما لم يُطلب خلاف ذلك صراحة.** لا حذف لبيانات قائمة (قاعات/تدريسيين/محاضرات) بدون تأكيد صريح من المستخدم.
2. **عند وجود غموض في تعيين بيانات (مثل تطابق اسم قاعة تقريبي)، اسأل المستخدم بدل الافتراض.** هذا صراحة ما طلبه المستخدم أكثر من مرة.
3. **الصلاحيات دائماً تُبنى فوق `canEditCollege`** بدل إنشاء نظام صلاحيات موازٍ — أي ميزة جديدة خاصة بكلية معينة يجب أن تُخفى/تُظهر حسب هذه الدالة نفسها.
4. **أي واجهة/تقرير جديد يحتاج شعار الجامعة يجب أن يستخدم `buildReportHead()`** (أو نفس أسلوبها: `<img>` حقيقي، ليس CSS background) — انظر القسم التالي "دروس مستفادة".
5. **كل تعديل على الجدول الأسبوعي أو التدريسيين يُختبر أولاً محلياً (Playwright + Firestore وهمي) قبل رفعه للموقع الحي.**

---

## 6. طريقة النشر الفعلية (مهم جداً — تغيّرت خلال هذا المشروع)

### آلية الاختبار المحلي (يجب الاستمرار عليها)
- نسخة محلية من الملف يُجرى عليها كل تعديل، ويُتحقق من صحتها بـ:
  - `node --check` على محتوى `<script>` المستخرج، للتأكد من خلو الكود من أخطاء صياغية.
  - اختبار Playwright بمتصفح Chromium مثبّت مسبقاً (`/opt/pw-browsers/chromium`)، مع **حقن Firestore وهمي** (`window.firebase` مزيّف بالكامل عبر `add_init_script`) يحاكي `doc/collection/onSnapshot/get/set/update/add/delete` — هذا يسمح باختبار كامل لتدفقات تسجيل الدخول/الصلاحيات/CRUD دون لمس القاعدة الحقيقية. (أمثلة كاملة جاهزة لإعادة الاستخدام في ملفات `smoke_test*.py` من الجلسات السابقة.)
  - لأي شيء متعلق بالطباعة: `page.emulate_media(media="print")` ثم لقطة شاشة، للتأكد من ظهور كل عنصر (خصوصاً الشعار) فعلياً في وضع الطباعة.

### آلية الرفع إلى GitHub (⚠️ تغيّرت ولم تعد تعمل تلقائياً)
- في جلسات سابقة كان بالإمكان الرفع مباشرة عبر GitHub Contents API (PUT) بتحقق SHA قبل/بعد.
- **لكن في هذه الجلسة تحديداً، لم تعد بيئة العمل السحابية تملك صلاحية push على هذا المستودع** (لا REST API ولا `git push` عبر HTTPS، رغم أن `git clone` للقراءة يعمل بلا مشاكل). ظهرت رسالة: *"access denied by the git proxy: ... is not in this session's authorized repository set"*.
- **الحل المُتّبع حالياً:** تجهيز الملف الكامل المحدَّث محلياً، اختباره، ثم تسليمه للمستخدم كملف كامل (`index.html`) ليقوم هو برفعه يدوياً عبر واجهة GitHub الإلكترونية (فتح الملف في المستودع → أيقونة التعديل → استبدال المحتوى بالكامل → Commit مباشرة على `main`).
- **⚠️ في محادثة جديدة: لا تفترض أن صلاحية push متاحة — تحقق أولاً بمحاولة `git clone` بسيطة، وإن فشل الدفع أخبر المستخدم فوراً واعرض عليه تسليم الملف بدل تكرار المحاولة.**
- **ملاحظة عن GitHub Pages:** بعد أي رفع ناجح، أول تحميل/تحديث للصفحة الحية كثيراً ما يُظهر نسخة قديمة مخبأة حتى مع إضافة رقم عشوائي في الرابط لتفادي الكاش — حتى `fetch(url,{cache:'no-store'})` قد يؤكد وصول المحتوى الجديد فعلاً بينما DOM المعروض لا يزال قديماً. **الحل المجرَّب مراراً: تحديث الصفحة مرة ثانية (إعادة تحميل كاملة جديدة)** يحل المشكلة دائماً تقريباً. أخبر المستخدم بذلك مسبقاً لتفادي الحيرة.

---

## 7. دروس مستفادة ومشاكل يجب تجنّب تكرارها

1. **لا تستخدم CSS `background-image` لأي عنصر يجب أن يظهر عند الطباعة.** أغلب المتصفحات (خصوصاً Chrome) لا تطبع خلفيات CSS افتراضياً إلا إذا فعّل المستخدم يدوياً خيار "Background graphics" في نافذة الطباعة — وهذا نادراً ما يحدث. **استخدم دائماً عنصر `<img>` حقيقي بمصدر (`src`) فعلي** لأي شعار/صورة يجب أن تظهر في تقرير مطبوع. هذا بالضبط ما سبّب شكوى المستخدم من غياب شعار الجامعة في التقارير، وتم إصلاحه باستبدال الـ `<div>` ذات الخلفية بـ `<img>` فعلية.
2. **لا تفترض صلاحية الرفع (push) على GitHub متاحة تلقائياً في كل جلسة.** تحقق مبكراً، وجهّز خطة بديلة (تسليم الملف للمستخدم) دون إبطاء العمل بانتظار حل تقني قد لا يتوفر.
3. **عند استخراج بيانات من ملف PDF لجدول محاضرات (خلايا مدمجة/matrix معقّد)، فضّل دائماً طلب نفس البيانات كملف Excel من المستخدم إن أمكن** بدل الاعتماد على استخراج بصري/إحداثيات بكسل من صورة الجدول — العملية بطيئة، عرضة للخطأ، وتستهلك وقتاً كبيراً مقارنة بجدول Excel منظم يصل مباشرة.
4. **الجدول الدراسي "نمط متكرر بلا تواريخ" — لا تخلط بينه وبين `bookings` (الذي له تواريخ فعلية).** أي طلب يذكر "فترة" أو "تاريخ" بخصوص الجدول الدراسي يحتاج توضيحاً صريحاً من المستخدم عن الشكل المطلوب (قالب أسبوعي مُكرَّر أم جدول تفصيلي بتواريخ فعلية مُسقطة) — في هذا المشروع اختار المستخدم صراحة "جدول تفصيلي بكل تاريخ فعلي" عبر دالة `expandRoomOccurrences`.
5. **اسأل دائماً قبل افتراض تعيين قاعة/اسم غامض** (تمّ هذا فعلياً أكثر من مرة بنجاح عبر أداة الأسئلة التوضيحية).
6. **تحقّق (SHA256) قبل وبعد أي تعديل محلي/رفع** لضمان تطابق الملف المحلي مع الحي، وتفادي الكتابة فوق تغييرات لم تُرَ بعد.
7. **لا تحذف بيانات قائمة إطلاقاً بدون طلب صريح.** كل الإضافات (قاعات جديدة، تدريسيون جدد) طُبّقت بشكل تراكمي (additive) فقط.

---

## 8. مهام/ملاحظات معلّقة من المشروع الأصلي (سياق فقط — قد لا تخص المشروع الجديد)

- بعد عرض النظام على مجلس الجامعة، يُطلب حذف كل مستندات الجدول/الحجوزات التي تحمل الحقل `demo:true` عبر كل الكليات.
- أسماء مسؤولي جدول الكليات (`config.people.college`) غير مكتملة لكليات: `den, phar, hmt, edu`.
- لم يُنجز بعد: استيراد الجدول الأسبوعي الكامل لكلية التمريض من ملف PDF (كان بانتظار ملف Excel بديل من المستخدم بسبب صعوبة الاستخراج من الـ PDF المدمج الخلايا).
- إمكانية تسوية أسماء قاعات كلية الصيدلة "ادهوك" مع القائمة الرسمية — أُشير إليها سابقاً لكن تُركت لتقدير المستخدم دون إجراء.

---

## 9. ملاحظة ختامية

هذا الملف يوثّق **حالة وسياق نظام العميد الإلكتروني للقاعات الدراسية كما هو الآن**، وليس متطلبات أي مشروع جديد. عند بدء محادثة جديدة مرتبطة بهذا النظام، سيشرح المستخدم تفاصيل المشروع الجديد بشكل منفصل — الدور المطلوب من هذا الملف هو فقط منح المحادثة الجديدة فهماً فورياً للبنية التقنية والاصطلاحات والدروس المستفادة، لتفادي إعادة اكتشافها أو تكرار نفس الأخطاء.
