# موجز المشروع — نظام العميد الإلكتروني لمتابعة العجلات

> ملف تسليم لجلسة Claude Code السحابية. اقرأه مع `ameed-system-project-context.md` (سياق نظام القاعات السابق) قبل البدء.
> المالك: مشتاق كريم — مساعد رئيس الجامعة للشؤون الإدارية والقانونية، جامعة العميد، كربلاء.

## 1. الفكرة
نظام ويب (يعمل جيداً على الهاتف) لمتابعة عجلات الجامعة:
- تفاصيل كل عجلة.
- صرفيات الوقود (بنزين / كاز).
- طلبات الصيانة.
- طلبات التنظيف والغسل.
- بيان بالطلبات المنجزة وغير المنجزة.
- تقرير لكل سائق: العجلات التي ساقها ونشاطه.
- تقرير لكل عجلة: صرفياتها من الوقود والتصليح.
- تقييم / نقاط لكل سائق.
- جدول آني للعجلات الموجودة داخل الجامعة والخارجة منها.

## 2. المستخدمون (الأدوار)
| الدور | الصلاحيات |
|---|---|
| admin (مدير النظام) | كل شيء، إدارة المستخدمين والمواقع والإعدادات |
| fleet (مدير الآليات) | إضافة وتعديل العجلات والسائقين، الموافقة على الطلبات، إغلاقها كمنجزة، تقييم السائقين |
| gate (الاستعلامات) | تسجيل خروج ودخول العجلات في موقعه أو مواقعه فقط، وعرض الجدول الآني |
| finance (المالية) | تأييد أو رفض المبالغ (وقود، صيانة، غسل) وتقارير مالية |
| driver (سائق) *مقترح* | تقديم طلبات وقود وصيانة وغسل من الهاتف، وعرض سجله ونقاطه |
| viewer (إدارة عليا) *مقترح* | عرض فقط، للوحة الإحصاءات والتقارير |

## 3. الالتزامات التقنية (من المشروع السابق)
- ملف واحد `index.html` (HTML + CSS + JS عادي، دون أي بناء أو إطار عمل)، ومساعد `el()`، وFirebase compat SDK، والنشر على GitHub Pages.
- نفس الخطوط (Amiri / Noto Naskh Arabic للعناوين، وTajawal للنص) ونفس الألوان والثيم الفاتح/الداكن، والاتجاه من اليمين لليسار بالكامل. **يُستنسخ المستودع العام `mushtaqaelk/ameed-room-booking` للقراءة ونقل الـ CSS والشعار وأسلوب `buildReportHead()` حرفياً.**
- الشعار في التقارير عنصر `<img>` حقيقي، وليس خلفية CSS.
- الاختبار: `node --check` واختبار Playwright مع Firestore وهمي، ولقطات بوضع الطباعة.
- التعديل على البيانات إضافي فقط. البيانات التجريبية تُعلَّم بـ `demo:true`.
- الواجهة مصممة للهاتف أولاً: أزرار كبيرة، ونماذج قصيرة، وتثبيت على الشاشة الرئيسية (PWA manifest)، وتفعيل Firestore offline persistence لموظف الاستعلامات عند ضعف الإنترنت.

## 4. الأسئلة مع الأجوبة المقترحة (المقترح هو الافتراض ما لم يغيّره المستخدم)

### أ. البنية
1. **قاعدة البيانات:** *المقترح:* مشروع Firebase جديد منفصل باسم `ameed-vehicles`، ينشئه المستخدم ويرسل الـ config. إلى أن يصل يُبنى النظام ويُختبر كاملاً على Firestore وهمي، فلا يتوقف العمل.
2. **العلاقة مع نظام VMD الحالي (WPF + Supabase لصيانة المركبات):** *المقترح:* نظام مستقل تماماً في المرحلة الأولى، ويُدرس الربط لاحقاً.
3. **تسجيل الدخول:** *المقترح:* حساب شخصي لكل موظف (اسم مستخدم + كلمة مرور عبر Firebase Auth، ينشئه الآدمن) بدل رمز مشترك لكل دور، ليُعرف من سجّل كل حركة (سجل تدقيق).
4. **دورا السائق والمشاهد الإضافيان:** *المقترح:* نعم، يُضافان.

### ب. الاستعلامات والجدول الآني
5. **مواقع الجامعة والبوابات:** *المقترح:* قائمة قابلة للتعديل من الآدمن، ويرسل المستخدم أسماء المواقع الفعلية لاحقاً. تُستخدم أسماء مؤقتة إلى حين وصولها.
6. **بيانات الحركة:** العجلة، والسائق الفعلي، والوقت (تلقائي)، والموقع، والاتجاه (خروج أو دخول)، وقراءة العداد (اختيارية)، والوجهة أو الغرض، وملاحظة. الجدول الآني يُحسب من آخر حركة لكل عجلة: داخل (وفي أي موقع) أو خارج (منذ متى ومع من). *المقترح:* كما ذُكر.
7. **السائق:** *المقترح:* لكل عجلة سائق افتراضي، لكن السائق الفعلي يُسجَّل في كل حركة خروج، ومنه تُبنى تقارير السائقين. يمكن كتابة اسم سائق من خارج القائمة.
8. **تنبيه:** *المقترح:* تنبيه للعجلة الخارجة منذ أكثر من X ساعة (افتراضياً 12 ساعة، قابل للتعديل).

### ج. الطلبات ودورة الموافقة
9. **الوقود:** لتر، ونوع (بنزين / كاز)، ومبلغ بالدينار، وقراءة العداد، والمحطة، وصورة الوصل (اختيارية). الدورة: *طلب (سائق أو مدير آليات) ← موافقة مدير الآليات ← تأييد المالية ← منجز*. يُحسب معدل الاستهلاك (كم/لتر) تلقائياً من قراءات العداد.
10. **الصيانة:** *جديد ← موافقة مدير الآليات ← قيد التنفيذ ← إدخال الكلفة الفعلية ← تأييد المالية ← منجز* (أو مرفوض في أي مرحلة مع سبب). العجلة تتحول تلقائياً إلى حالة "في الصيانة" أثناء التنفيذ.
11. **الغسل والتنظيف:** دورة أبسط: *طلب ← منجز*، وإذا كانت له كلفة يمر على المالية.
12. **طريقة الدفع الفعلية للوقود** (نقداً مع استرداد، أو كوبونات، أو حساب لدى محطة): *المقترح:* حقل "طريقة الدفع" بثلاث قيم، ولا يتغير المنطق.
13. **الصور والمرفقات:** Firebase Storage يتطلب الآن خطة Blaze المدفوعة. *المقترح:* ضغط الصورة في المتصفح (نحو 150KB) وحفظها داخل Firestore، بلا حاجة لبطاقة دفع.

### د. تفاصيل العجلة
14. **الحقول:** رقم اللوحة والمحافظة، والنوع والموديل والسنة، واللون، ورقم الشاصي، ونوع الوقود، والعداد الحالي، وتاريخ انتهاء السنوية، وتاريخ انتهاء التأمين، والحالة (فعّالة / في الصيانة / عاطلة)، والجهة المستفيدة، وصورة، وملاحظات. *المقترح:* هذه الحقول، مع تنبيهات لقرب انتهاء السنوية والتأمين، ولتبديل الدهن كل X كم.

### هـ. تقييم السائقين
15. *المقترح:* رصيد شهري يبدأ بـ 100 نقطة.
   - **خصم تلقائي:** تأخر إرجاع العجلة، واستهلاك وقود أعلى من معدل العجلة بنسبة كبيرة.
   - **خصم أو إضافة يدوية من مدير الآليات:** مخالفة، أو حادث، أو التزام متميز، مع سبب مكتوب.
   - **تقييم بالنجوم (1–5):** يمنحه مدير الآليات شهرياً.
   - **لوحة ترتيب السائقين:** تعرض ترتيبهم حسب النقاط.

### و. التقارير والعرض
16. **التقارير:** تقرير عجلة (لفترة زمنية)، وتقرير سائق، وملخص مالي شهري، وبيان بالطلبات المنجزة وغير المنجزة، وسجل الحركة. كلها قابلة للطباعة بـ `buildReportHead()`، مع تصدير Excel. *المقترح:* نعم.
17. **الأرقام:** *المقترح:* أرقام غربية (0–9) للمبالغ واللوحات والعدادات، وعملة الدينار العراقي بفواصل الآلاف.
18. **البيانات الأولية:** *المقترح:* يرسل المستخدم لاحقاً ملف Excel بالعجلات والسائقين، ويُبنى استيراد من Excel. حتى ذلك الحين تُستخدم بيانات تجريبية `demo:true`.
19. **الإشعارات:** *المقترح:* عدادات داخل النظام بالطلبات المعلقة لكل دور. إشعارات واتساب وتلغرام خارج المرحلة الأولى.

## 5. خطة التنفيذ
1. استنساخ `ameed-room-booking` للقراءة ونقل الثيم والشعار.
2. هيكل `index.html`: تسجيل الدخول والأدوار، وتبويبات: الرئيسية (الجدول الآني والتنبيهات)، والحركة (الاستعلامات)، والطلبات، والعجلات، والسائقون، والمالية، والتقارير، والإدارة.
3. نموذج البيانات: `config/general` (المواقع والإعدادات)، و`users`، و`vehicles`، و`drivers`، و`movements`، و`requests` (type: fuel|maintenance|wash)، و`driverPoints`، و`audit`.
4. قواعد أمان Firestore حسب الدور (`firestore.rules` في المستودع).
5. اختبارات Playwright لكل دور، ولقطات هاتف (375px) وطباعة.
6. الرفع إلى المستودع وتفعيل GitHub Pages، مع تعليمات للمستخدم لإنشاء مشروع Firebase ولصق الـ config.
